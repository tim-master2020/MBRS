package com.example.demo.user.repositories;

import org.springframework.stereotype.Repository;
import com.example.demo.generated.repositories.${class.name}BaseRepository;

@Repository
public interface ${class.name}Repository extends ${class.name}BaseRepository {

}